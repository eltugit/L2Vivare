# ---------------------------------------------------------------------------
# ---- Leader board SETTINGS (same for all boards) for L2JSunrise ----
# ---------------------------------------------------------------------------

#This parameter is to turn on/off the Event Engine at server start.
#If True, it writes into gameserver console: LeaderBoards: Activated.
#Also will start auto event manager.
#If False, it writes into gameserver console: LeaderBoards: Disabled.
Enableleaderboard = False

# ---------------------------------------------------------------------------
# Arena LeaderBoards
# ---------------------------------------------------------------------------
# Arena Manager Configs
# Required: CustomNpcTable = True
RankArenaEnabled = False
# Interval in minutes
RankArenaInterval = 3600
# Reward ID
RankArenaRewardId = 57
# Reward amount 0 to disable
RankArenaRewardCount = 1000

# Anti Dual-Box in the event:
ArenaAcceptSameIP = True

# ---------------------------------------------------------------------------
# Fisherman LeaderBoards
# ---------------------------------------------------------------------------
# Fish Manager configs
# Required: CustomNpcTable = True
RankFishermanEnabled = False
# Interval in minutes
RankFishermanInterval = 3600
# Reward ID
RankFishermanRewardId = 57
# Reward amount 0 to disable
RankFishermanRewardCount = 1000

# ---------------------------------------------------------------------------
# Craft LeaderBoards
# ---------------------------------------------------------------------------
# Craft Manager configs
# Required: CustomNpcTable = True
RankCraftEnabled = False
# Interval in minutes
RankCraftInterval = 3600
# Reward ID
RankCraftRewardId = 57
# Reward amount 0 to disable
RankCraftRewardCount = 1000

# ---------------------------------------------------------------------------
# TvT LeaderBoards
# ---------------------------------------------------------------------------
# TvT Manager configs
# Required: CustomNpcTable = True
RankTvTEnabled = False
# Interval in minutes
RankTvTInterval = 3600
# Reward ID
RankTvTRewardId = 57
# Reward amount 0 to disable
RankTvTRewardCount = 1000